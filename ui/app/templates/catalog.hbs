{{page-title "Catalog"}}
<p>
  Select a package to configure:
</p>

<ul>
  {{#each @model as |package|}}
    <li>
      <LinkTo @route="package" @model={{package.Name}}>{{package.Name}}</LinkTo>
    </li>
  {{/each}}
</ul>
